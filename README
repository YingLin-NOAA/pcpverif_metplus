2019/01/27: from https://github.com/NCAR/METplus/archive/v3.0-beta2.zip

From Logan, 1/24:
  In the beta's ush directory, the validate_config.py script is what you'll use to check for deprecated configs. You can run it instead of master_metplus.py (passing in all the same .conf files), and it'll check and updated the deprecated settings.

